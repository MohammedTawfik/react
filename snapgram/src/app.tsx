import { Route, Routes } from "react-router-dom";

const app = () => {
  return (
    <main className="flex h-screen">
      {/* Public Routes (any one can access) */}

      {/* Private Routes (only register users can access) */}
    </main>
  );
};

export default app;
